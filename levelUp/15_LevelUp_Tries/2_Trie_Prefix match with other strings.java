// LU: Trie_Prefix match with other strings
// Success rate: 37.50%
// Given an array of strings arr[] of size n, a string str, and an integer k, the task is to find how many strings in the array arr[] have a prefix of length k that matches the k-length prefix of str.

// Input Format:
// The first line contains an integer n (number of strings in the array arr[]).
// The second line contains n space-separated strings representing the array arr[].
// The third line contains an integer k (length of the prefix to match).
// The fourth line contains the string str whose k-length prefix needs to be compared with the prefixes of strings in arr[].

// Output Format:
// Print a single integer representing the count of strings in arr[] whose prefix of length k matches the k-length prefix of str.

// Constraints:
// 1 <= n <= 1000
// 1 <= |arr[i]| , |str| <= 1000
// 1 <= k <= 1000
// arr[i], str must contain only lowercase English alphabets